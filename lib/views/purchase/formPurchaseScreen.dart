import 'package:flutter/material.dart';
class FormPurchaseScreen extends StatelessWidget {
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
         child: Text('FormPurchaseScreen'),
      ),
    );
  }
}